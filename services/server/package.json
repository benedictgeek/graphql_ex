{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --watch & nodemon index.js",
    "dev": "(kill -9 $(lsof -ti:4040) || echo ) && tsc --watch & nodemon index.js"
  },
  "author": "Olushola",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "express-graphql": "^0.12.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "nodemon": "^2.0.19",
    "tsconfig": "workspace:*"
  }
}
